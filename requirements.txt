# Convenience mirror of pyproject.toml for `pip install -r requirements.txt`.
# pyproject.toml is the source of truth for version bounds; prefer
# `pip install -e ".[dev,anthropic]"` for development.
PyYAML>=6.0,<7.0
anthropic>=0.40,<1.0
